import java.awt.*;

public class hashcode {

    public static void main(String[] args) {/*
        String a="100";
        String b="2";

        System.out.println("HashCode of a="+a+":"+a.hashCode());
        System.out.println("HashCode of b="+b+":"+b.hashCode());*/

        //data fields and methods
        //object and classes
        //constructors-> simply a method
        //API based project-> google map, amazon,google->html,css,javascript
        //portfolio based project-> fronted development, UI
        //FULL Stack project->clone of facebook,twitter
        //blockchain project
        /*
        public Point(int x,int y){
            this.x=x;
            this.y=y;
        }

            //neso academy

        Point p1=new Point(0,1);*/
        //default constructor

        //GUI (graphical user interface)
        //Label, Button etc

        //creating a class
        //class, attributes and methods

        //Method-> getArea(), getPerimeter() and setCenter(Point) setRadius(newRadius)

        //instantiating objects
        //the new keyboard

        //default value of a character is '\u0000'

       //Circle c1=new Circle();//default constructor
      //  c1.center= new Point(0,0);
      //  c1.radius=3;
        //by using dot operator, we can access it now, but if it is made private,
        //then we have to access it through method
        //c1.setCenter(new Point(1,2));
       // c1.setRadius(5);
        Circle c1=new Circle();
        System.out.println(c1.radius);
        System.out.println(c1.center);

        Circle c2=new Circle(new Point(1,2),3);

        System.out.println(c2.radius);
        System.out.println(c2.center);

        //System.out.println(c1.getArea());
        //System.out.println(c1.getPerimeter());

        //again constructor overloading
        Point p1= new Point();
        System.out.println(p1);

        Point p2=new Point(1,2);
        System.out.println(p2);

        //constructors, default constructors and constructors overloading

        //constructors must have the same name as class
        //do not have any return type
        //invoked using the new operator
        //constructors play the role of initializing objects

        //static variables and static methods in java

        //static variables and methods belong to the class, they are shared
        //between all objects

        























    }
}
