package Neso2;

public class main {
    public static void main(String[] args) {

        C1 x=new C1();
        x.setX(10);
        System.out.println(x.getX());
        //immutable classes and objects
        //immutable objects-> can not be changed
        //example-- string
        //all data field must be private
        // there can't be setter methods because it is fixed
        //this keyboard -> referencing instance members using this,
        // invoking an overloaded constructor
        //this and static variables
        // we can not use this in static variable and method









    }
}
